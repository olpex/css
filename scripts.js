const text = document.getElementsByTagName("p");
for (let i = 0; i < text.length; i++) {
  text[i].style.color = "red";
}

const textElement = getElementsByTagName("p");
const textContent = textElement.innerHTML();
console.log(textContent);
